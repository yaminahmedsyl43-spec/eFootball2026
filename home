<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Yamin Ahmed Vlog – eFootball Tips, News & Meta Guides</title>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@400;500;600;700&family=Exo+2:wght@300;400;600&display=swap" rel="stylesheet">
<style>
  :root {
    --purple: #8A2BE2;
    --neon-blue: #00E5FF;
    --black: #0A0A0A;
    --green: #00FF6A;
    --pink: #FF0077;
    --white: #EAEAEA;
    --deep-purple: #1B0033;
    --card-bg: rgba(255,255,255,0.04);
    --border: rgba(0,229,255,0.2);
  }

  * { margin:0; padding:0; box-sizing:border-box; }

  body {
    font-family: 'Exo 2', sans-serif;
    background: linear-gradient(135deg, #0A0A0A 0%, #0d001a 50%, #1B0033 100%);
    background-attachment: fixed;
    color: var(--white);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* GRID BACKGROUND */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(0,229,255,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,229,255,0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    pointer-events: none;
    z-index: 0;
  }

  /* ============ NAVBAR ============ */
  header {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: rgba(10,10,10,0.9);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
    padding: 0 2rem;
  }

  .nav-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.8rem 0;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    text-decoration: none;
  }

  .logo-svg {
    width: 52px;
    height: 52px;
    flex-shrink: 0;
  }

  .logo-text {
    display: flex;
    flex-direction: column;
  }

  .logo-main {
    font-family: 'Orbitron', sans-serif;
    font-size: 1.1rem;
    font-weight: 900;
    color: var(--neon-blue);
    letter-spacing: 1px;
    text-shadow: 0 0 20px rgba(0,229,255,0.5);
    line-height: 1.1;
  }

  .logo-sub {
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.65rem;
    font-weight: 600;
    color: var(--green);
    letter-spacing: 3px;
    text-transform: uppercase;
  }

  .nav-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .btn-yt {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: linear-gradient(135deg, var(--pink), var(--purple));
    color: #fff;
    padding: 0.5rem 1.2rem;
    border-radius: 30px;
    text-decoration: none;
    font-family: 'Rajdhani', sans-serif;
    font-weight: 700;
    font-size: 0.85rem;
    letter-spacing: 0.5px;
    box-shadow: 0 0 20px rgba(255,0,119,0.4);
    transition: all 0.3s;
  }

  .btn-yt:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 35px rgba(255,0,119,0.7);
  }

  .btn-admin {
    background: transparent;
    border: 1px solid var(--green);
    color: var(--green);
    padding: 0.5rem 1rem;
    border-radius: 6px;
    cursor: pointer;
    font-family: 'Rajdhani', sans-serif;
    font-weight: 700;
    font-size: 0.8rem;
    letter-spacing: 1px;
    transition: all 0.3s;
  }

  .btn-admin:hover {
    background: var(--green);
    color: #000;
  }

  /* NAV SCROLL */
  .nav-scroll {
    overflow-x: auto;
    scrollbar-width: none;
    padding: 0.5rem 0;
    border-top: 1px solid rgba(255,255,255,0.05);
  }

  .nav-scroll::-webkit-scrollbar { display: none; }

  .nav-links {
    display: flex;
    gap: 0.3rem;
    width: max-content;
  }

  .nav-links a {
    text-decoration: none;
    color: rgba(234,234,234,0.7);
    font-family: 'Rajdhani', sans-serif;
    font-weight: 600;
    font-size: 0.78rem;
    letter-spacing: 0.5px;
    padding: 0.3rem 0.8rem;
    border-radius: 4px;
    white-space: nowrap;
    transition: all 0.2s;
    text-transform: uppercase;
  }

  .nav-links a:hover {
    color: var(--neon-blue);
    background: rgba(0,229,255,0.08);
  }

  /* ============ HERO ============ */
  .hero {
    position: relative;
    z-index: 1;
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 4rem 2rem;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    width: 700px;
    height: 700px;
    background: radial-gradient(circle, rgba(138,43,226,0.25) 0%, transparent 70%);
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
  }

  .hero-glow2 {
    position: absolute;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(0,229,255,0.15) 0%, transparent 70%);
    right: 10%;
    top: 20%;
    pointer-events: none;
    animation: pulse 4s ease-in-out infinite;
  }

  @keyframes pulse {
    0%,100% { opacity: 0.5; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.1); }
  }

  .hero-content {
    position: relative;
    z-index: 2;
  }

  .hero-badge {
    display: inline-block;
    background: rgba(0,229,255,0.1);
    border: 1px solid var(--neon-blue);
    color: var(--neon-blue);
    padding: 0.3rem 1rem;
    border-radius: 30px;
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 1.5rem;
    animation: glow-border 2s ease-in-out infinite;
  }

  @keyframes glow-border {
    0%,100% { box-shadow: 0 0 10px rgba(0,229,255,0.3); }
    50% { box-shadow: 0 0 25px rgba(0,229,255,0.7); }
  }

  .hero h1 {
    font-family: 'Orbitron', sans-serif;
    font-size: clamp(2rem, 5vw, 4rem);
    font-weight: 900;
    color: var(--neon-blue);
    text-shadow: 0 0 40px rgba(0,229,255,0.4);
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .hero h1 span {
    color: var(--green);
    text-shadow: 0 0 30px rgba(0,255,106,0.4);
  }

  .hero-sub {
    font-size: 1.1rem;
    color: rgba(234,234,234,0.8);
    margin-bottom: 2.5rem;
    font-family: 'Rajdhani', sans-serif;
    font-size: 1.2rem;
    letter-spacing: 1px;
  }

  .btn-subscribe {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    background: linear-gradient(135deg, var(--purple), var(--pink));
    color: #fff;
    padding: 0.9rem 2.5rem;
    border-radius: 50px;
    text-decoration: none;
    font-family: 'Orbitron', sans-serif;
    font-weight: 700;
    font-size: 0.9rem;
    box-shadow: 0 0 40px rgba(138,43,226,0.5);
    transition: all 0.3s;
    letter-spacing: 1px;
  }

  .btn-subscribe:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 0 60px rgba(255,0,119,0.7);
  }

  .hero-stats {
    display: flex;
    gap: 3rem;
    justify-content: center;
    margin-top: 3rem;
  }

  .stat {
    text-align: center;
  }

  .stat-num {
    font-family: 'Orbitron', sans-serif;
    font-size: 1.8rem;
    font-weight: 900;
    color: var(--green);
    text-shadow: 0 0 20px rgba(0,255,106,0.5);
  }

  .stat-label {
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.7rem;
    color: rgba(234,234,234,0.5);
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  /* ============ SECTIONS ============ */
  section {
    position: relative;
    z-index: 1;
    padding: 4rem 2rem;
    max-width: 1400px;
    margin: 0 auto;
  }

  .section-header {
    margin-bottom: 2.5rem;
  }

  .section-label {
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.7rem;
    font-weight: 700;
    color: var(--green);
    letter-spacing: 4px;
    text-transform: uppercase;
    margin-bottom: 0.4rem;
  }

  .section-title {
    font-family: 'Orbitron', sans-serif;
    font-size: clamp(1.4rem, 3vw, 2rem);
    font-weight: 700;
    color: var(--neon-blue);
  }

  /* ============ CARDS ============ */
  .cards-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  .post-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 12px;
    overflow: hidden;
    transition: all 0.3s;
    cursor: pointer;
  }

  .post-card:hover {
    transform: translateY(-5px);
    border-color: var(--neon-blue);
    box-shadow: 0 10px 40px rgba(0,229,255,0.15);
  }

  .card-img {
    width: 100%;
    height: 180px;
    object-fit: cover;
    background: linear-gradient(135deg, #1a0030, #0a1530);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
    position: relative;
    overflow: hidden;
  }

  .card-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .card-img-placeholder {
    width: 100%;
    height: 180px;
    background: linear-gradient(135deg, #1a0030, #0a1530);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
  }

  .card-tag {
    position: absolute;
    top: 10px;
    left: 10px;
    background: var(--purple);
    color: #fff;
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 2px;
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
    text-transform: uppercase;
  }

  .card-body {
    padding: 1.2rem;
  }

  .card-title {
    font-family: 'Rajdhani', sans-serif;
    font-size: 1rem;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 0.4rem;
    line-height: 1.3;
  }

  .card-desc {
    font-size: 0.8rem;
    color: rgba(234,234,234,0.6);
    line-height: 1.5;
    margin-bottom: 0.8rem;
  }

  .card-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.7rem;
    color: var(--green);
    letter-spacing: 1px;
  }

  /* ============ CATEGORIES ============ */
  .cats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 1rem;
  }

  .cat-btn {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 1.2rem 1rem;
    text-align: center;
    cursor: pointer;
    transition: all 0.3s;
    text-decoration: none;
    color: var(--white);
    display: block;
  }

  .cat-btn:hover {
    transform: translateY(-4px);
    border-color: var(--green);
    box-shadow: 0 8px 30px rgba(0,255,106,0.2);
  }

  .cat-icon {
    font-size: 1.8rem;
    display: block;
    margin-bottom: 0.5rem;
  }

  .cat-name {
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--neon-blue);
    letter-spacing: 0.5px;
    text-transform: uppercase;
  }

  /* ============ FEATURED VIDEO ============ */
  .featured-video-wrap {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 16px;
    overflow: hidden;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
  }

  @media(max-width:768px) {
    .featured-video-wrap { grid-template-columns: 1fr; }
  }

  .video-embed {
    background: #000;
    min-height: 280px;
  }

  .video-embed iframe {
    width: 100%;
    height: 100%;
    min-height: 280px;
    border: none;
    display: block;
  }

  .video-info {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .video-tag {
    display: inline-block;
    background: var(--pink);
    color: #fff;
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 2px;
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
    margin-bottom: 1rem;
    text-transform: uppercase;
  }

  .video-title {
    font-family: 'Orbitron', sans-serif;
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--neon-blue);
    margin-bottom: 1rem;
    line-height: 1.4;
  }

  .video-desc {
    color: rgba(234,234,234,0.7);
    font-size: 0.85rem;
    line-height: 1.6;
  }

  /* ============ COMMUNITY / SQUAD SHOWCASE ============ */
  .showcase-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1.2rem;
  }

  .showcase-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 10px;
    overflow: hidden;
    transition: all 0.3s;
  }

  .showcase-card:hover {
    border-color: var(--pink);
    box-shadow: 0 8px 30px rgba(255,0,119,0.15);
  }

  .showcase-img {
    width: 100%;
    height: 150px;
    background: linear-gradient(135deg, #200040, #001030);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    position: relative;
    overflow: hidden;
  }

  .showcase-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .showcase-body {
    padding: 1rem;
  }

  .showcase-title {
    font-family: 'Rajdhani', sans-serif;
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--white);
    margin-bottom: 0.5rem;
  }

  .upvote-row {
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }

  .upvote-btn {
    background: transparent;
    border: 1px solid var(--green);
    color: var(--green);
    padding: 0.3rem 0.8rem;
    border-radius: 4px;
    cursor: pointer;
    font-family: 'Rajdhani', sans-serif;
    font-weight: 700;
    font-size: 0.75rem;
    transition: all 0.2s;
  }

  .upvote-btn:hover, .upvote-btn.active {
    background: var(--green);
    color: #000;
  }

  .upvote-count {
    font-family: 'Orbitron', sans-serif;
    font-size: 0.8rem;
    color: var(--green);
  }

  .community-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
  }

  .btn-upload {
    background: linear-gradient(135deg, var(--purple), var(--neon-blue));
    color: #fff;
    padding: 0.7rem 1.5rem;
    border-radius: 8px;
    border: none;
    cursor: pointer;
    font-family: 'Rajdhani', sans-serif;
    font-weight: 700;
    font-size: 0.9rem;
    letter-spacing: 1px;
    transition: all 0.3s;
  }

  .btn-upload:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(138,43,226,0.5);
  }

  /* ============ FOOTER ============ */
  footer {
    position: relative;
    z-index: 1;
    background: rgba(0,0,0,0.6);
    border-top: 1px solid var(--border);
    padding: 3rem 2rem 2rem;
    margin-top: 4rem;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 3rem;
    max-width: 1400px;
    margin: 0 auto 2rem;
  }

  @media(max-width:768px) { .footer-grid { grid-template-columns: 1fr; } }

  .footer-about p {
    color: rgba(234,234,234,0.6);
    font-size: 0.85rem;
    line-height: 1.6;
    margin-top: 0.8rem;
  }

  .footer-title {
    font-family: 'Orbitron', sans-serif;
    font-size: 0.8rem;
    color: var(--green);
    margin-bottom: 1rem;
    letter-spacing: 2px;
  }

  .footer-links {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .footer-links a {
    color: rgba(234,234,234,0.6);
    text-decoration: none;
    font-size: 0.82rem;
    transition: color 0.2s;
    font-family: 'Rajdhani', sans-serif;
    font-weight: 500;
  }

  .footer-links a:hover { color: var(--neon-blue); }

  .footer-bottom {
    text-align: center;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(255,255,255,0.05);
    color: rgba(234,234,234,0.4);
    font-size: 0.75rem;
    font-family: 'Rajdhani', sans-serif;
    max-width: 1400px;
    margin: 0 auto;
  }

  /* ============ ADMIN PANEL ============ */
  .admin-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.85);
    z-index: 5000;
    display: none;
    overflow-y: auto;
  }

  .admin-overlay.open { display: block; }

  .admin-panel {
    background: #0d0020;
    border: 1px solid var(--purple);
    border-radius: 16px;
    max-width: 900px;
    margin: 2rem auto;
    overflow: hidden;
    box-shadow: 0 0 60px rgba(138,43,226,0.4);
  }

  .admin-header {
    background: linear-gradient(135deg, var(--purple), #3d00a8);
    padding: 1.5rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .admin-header h2 {
    font-family: 'Orbitron', sans-serif;
    font-size: 1.1rem;
    color: #fff;
    letter-spacing: 2px;
  }

  .admin-close {
    background: rgba(255,255,255,0.1);
    border: none;
    color: #fff;
    width: 36px;
    height: 36px;
    border-radius: 8px;
    cursor: pointer;
    font-size: 1.1rem;
    transition: background 0.2s;
  }

  .admin-close:hover { background: var(--pink); }

  .admin-body {
    padding: 2rem;
  }

  .admin-tabs {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
    border-bottom: 1px solid var(--border);
    padding-bottom: 1rem;
  }

  .admin-tab {
    background: transparent;
    border: 1px solid var(--border);
    color: rgba(234,234,234,0.6);
    padding: 0.4rem 0.9rem;
    border-radius: 6px;
    cursor: pointer;
    font-family: 'Rajdhani', sans-serif;
    font-weight: 600;
    font-size: 0.75rem;
    letter-spacing: 0.5px;
    transition: all 0.2s;
    text-transform: uppercase;
  }

  .admin-tab.active, .admin-tab:hover {
    background: var(--purple);
    border-color: var(--purple);
    color: #fff;
  }

  .tab-content { display: none; }
  .tab-content.active { display: block; }

  .form-group {
    margin-bottom: 1.2rem;
  }

  .form-group label {
    display: block;
    font-family: 'Rajdhani', sans-serif;
    font-weight: 700;
    font-size: 0.8rem;
    color: var(--neon-blue);
    letter-spacing: 1px;
    text-transform: uppercase;
    margin-bottom: 0.4rem;
  }

  .form-group input,
  .form-group textarea,
  .form-group select {
    width: 100%;
    background: rgba(255,255,255,0.04);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.7rem 1rem;
    color: var(--white);
    font-family: 'Exo 2', sans-serif;
    font-size: 0.88rem;
    outline: none;
    transition: border-color 0.2s;
  }

  .form-group input:focus,
  .form-group textarea:focus,
  .form-group select:focus {
    border-color: var(--purple);
    box-shadow: 0 0 15px rgba(138,43,226,0.2);
  }

  .form-group textarea { resize: vertical; min-height: 90px; }
  .form-group select option { background: #0d0020; }

  .image-upload-area {
    border: 2px dashed var(--border);
    border-radius: 10px;
    padding: 1.5rem;
    text-align: center;
    cursor: pointer;
    transition: all 0.3s;
    position: relative;
  }

  .image-upload-area:hover {
    border-color: var(--purple);
    background: rgba(138,43,226,0.05);
  }

  .image-upload-area input[type="file"] {
    position: absolute;
    inset: 0;
    opacity: 0;
    cursor: pointer;
    width: 100%;
    height: 100%;
  }

  .upload-icon { font-size: 2rem; margin-bottom: 0.5rem; }

  .upload-text {
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.85rem;
    color: rgba(234,234,234,0.6);
  }

  .upload-text span {
    color: var(--neon-blue);
    font-weight: 700;
  }

  .image-preview-box {
    margin-top: 1rem;
    border-radius: 8px;
    overflow: hidden;
    display: none;
    max-height: 200px;
  }

  .image-preview-box img {
    width: 100%;
    height: 200px;
    object-fit: cover;
  }

  .image-preview-box.show { display: block; }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  @media(max-width:600px) { .form-row { grid-template-columns: 1fr; } }

  .btn-submit {
    background: linear-gradient(135deg, var(--purple), var(--pink));
    color: #fff;
    border: none;
    padding: 0.8rem 2rem;
    border-radius: 8px;
    cursor: pointer;
    font-family: 'Orbitron', sans-serif;
    font-weight: 700;
    font-size: 0.8rem;
    letter-spacing: 1px;
    transition: all 0.3s;
    margin-top: 0.5rem;
  }

  .btn-submit:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(138,43,226,0.5);
  }

  .admin-posts-list {
    margin-top: 2rem;
  }

  .admin-post-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.8rem 1rem;
    background: rgba(255,255,255,0.03);
    border: 1px solid var(--border);
    border-radius: 8px;
    margin-bottom: 0.6rem;
    transition: all 0.2s;
  }

  .admin-post-item:hover {
    border-color: rgba(0,229,255,0.4);
  }

  .admin-post-thumb {
    width: 50px;
    height: 40px;
    border-radius: 4px;
    background: linear-gradient(135deg, #200040, #001030);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    flex-shrink: 0;
    overflow: hidden;
  }

  .admin-post-thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .admin-post-info {
    flex: 1;
  }

  .admin-post-title {
    font-family: 'Rajdhani', sans-serif;
    font-weight: 700;
    font-size: 0.85rem;
    color: var(--white);
  }

  .admin-post-cat {
    font-size: 0.7rem;
    color: var(--green);
    font-family: 'Rajdhani', sans-serif;
    letter-spacing: 1px;
  }

  .admin-post-actions {
    display: flex;
    gap: 0.5rem;
  }

  .btn-delete {
    background: transparent;
    border: 1px solid var(--pink);
    color: var(--pink);
    padding: 0.25rem 0.6rem;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.7rem;
    font-family: 'Rajdhani', sans-serif;
    font-weight: 700;
    transition: all 0.2s;
  }

  .btn-delete:hover { background: var(--pink); color: #fff; }

  /* ============ PASSWORD MODAL ============ */
  .pw-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.92);
    z-index: 6000;
    display: none;
    align-items: center;
    justify-content: center;
  }
  .pw-overlay.open { display: flex; }

  .pw-box {
    background: #0d0020;
    border: 1px solid var(--purple);
    border-radius: 16px;
    padding: 2.5rem 2rem;
    width: 360px;
    max-width: 92vw;
    text-align: center;
    box-shadow: 0 0 60px rgba(138,43,226,0.5);
    animation: pw-drop 0.3s ease;
  }

  @keyframes pw-drop {
    from { transform: translateY(-30px); opacity: 0; }
    to   { transform: translateY(0);     opacity: 1; }
  }

  .pw-icon {
    font-size: 2.5rem;
    margin-bottom: 0.8rem;
  }

  .pw-title {
    font-family: 'Orbitron', sans-serif;
    font-size: 1rem;
    color: var(--neon-blue);
    margin-bottom: 0.4rem;
    letter-spacing: 2px;
  }

  .pw-sub {
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.8rem;
    color: rgba(234,234,234,0.5);
    margin-bottom: 1.5rem;
    letter-spacing: 1px;
  }

  .pw-input-wrap {
    position: relative;
    margin-bottom: 1.2rem;
  }

  .pw-input {
    width: 100%;
    background: rgba(255,255,255,0.05);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.75rem 2.8rem 0.75rem 1rem;
    color: var(--white);
    font-family: 'Exo 2', sans-serif;
    font-size: 1rem;
    outline: none;
    letter-spacing: 3px;
    text-align: center;
    transition: border-color 0.2s;
  }

  .pw-input:focus { border-color: var(--purple); box-shadow: 0 0 15px rgba(138,43,226,0.3); }
  .pw-input.error { border-color: var(--pink); box-shadow: 0 0 15px rgba(255,0,119,0.3); animation: shake 0.3s; }

  @keyframes shake {
    0%,100% { transform: translateX(0); }
    25% { transform: translateX(-8px); }
    75% { transform: translateX(8px); }
  }

  .pw-toggle {
    position: absolute;
    right: 10px;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    color: rgba(234,234,234,0.5);
    cursor: pointer;
    font-size: 1rem;
    line-height: 1;
  }

  .pw-err {
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.8rem;
    color: var(--pink);
    margin-bottom: 1rem;
    letter-spacing: 1px;
    min-height: 1.2rem;
  }

  .pw-submit {
    width: 100%;
    background: linear-gradient(135deg, var(--purple), var(--pink));
    color: #fff;
    border: none;
    padding: 0.85rem;
    border-radius: 8px;
    cursor: pointer;
    font-family: 'Orbitron', sans-serif;
    font-weight: 700;
    font-size: 0.85rem;
    letter-spacing: 2px;
    transition: all 0.3s;
    margin-bottom: 1rem;
  }

  .pw-submit:hover { box-shadow: 0 8px 25px rgba(138,43,226,0.6); transform: translateY(-2px); }

  .pw-cancel {
    background: none;
    border: none;
    color: rgba(234,234,234,0.4);
    cursor: pointer;
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.8rem;
    letter-spacing: 1px;
    transition: color 0.2s;
  }
  .pw-cancel:hover { color: var(--white); }

  .pw-attempts {
    margin-top: 0.8rem;
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.72rem;
    color: rgba(234,234,234,0.3);
    letter-spacing: 1px;
  }

  /* ============ LOCKED STATE ============ */
  .admin-locked-banner {
    background: rgba(255,0,119,0.08);
    border: 1px solid var(--pink);
    border-radius: 8px;
    padding: 1rem 1.5rem;
    text-align: center;
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.85rem;
    color: var(--pink);
    letter-spacing: 1px;
    display: none;
    margin: 1rem;
  }

  .success-toast {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    background: var(--green);
    color: #000;
    padding: 0.8rem 1.5rem;
    border-radius: 8px;
    font-family: 'Orbitron', sans-serif;
    font-size: 0.8rem;
    font-weight: 700;
    z-index: 9999;
    transform: translateY(100px);
    opacity: 0;
    transition: all 0.4s;
  }

  .success-toast.show {
    transform: translateY(0);
    opacity: 1;
  }

  /* MOBILE */
  @media(max-width:768px) {
    .hero-stats { gap: 1.5rem; }
    .stat-num { font-size: 1.3rem; }
    .nav-top { flex-wrap: wrap; gap: 0.5rem; }
  }

  /* ANIMATED LINE SEPARATOR */
  .neon-sep {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--purple), var(--neon-blue), var(--green), transparent);
    margin: 0 2rem;
    opacity: 0.4;
  }

  /* EMPTY STATE */
  .empty-state {
    text-align: center;
    padding: 3rem;
    color: rgba(234,234,234,0.4);
    font-family: 'Rajdhani', sans-serif;
    font-size: 0.9rem;
    letter-spacing: 1px;
  }

  .empty-state .big-icon { font-size: 3rem; margin-bottom: 1rem; }
</style>
</head>
<body>

<!-- HEADER -->
<header>
  <div class="nav-top">
    <a href="#" class="logo">
      <!-- LOGO SVG -->
      <svg class="logo-svg" viewBox="0 0 52 52" fill="none" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <linearGradient id="lg1" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#00E5FF"/>
            <stop offset="100%" stop-color="#8A2BE2"/>
          </linearGradient>
          <linearGradient id="lg2" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#00FF6A"/>
            <stop offset="100%" stop-color="#00E5FF"/>
          </linearGradient>
          <filter id="glow">
            <feGaussianBlur stdDeviation="1.5" result="blur"/>
            <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
          </filter>
        </defs>
        <!-- Shield base -->
        <path d="M26 3 L46 12 L46 28 C46 38 36 46 26 49 C16 46 6 38 6 28 L6 12 Z" fill="url(#lg1)" opacity="0.15" stroke="url(#lg1)" stroke-width="1.5"/>
        <!-- Inner hex -->
        <path d="M26 8 L42 16 L42 28 C42 36 34 43 26 46 C18 43 10 36 10 28 L10 16 Z" fill="none" stroke="url(#lg2)" stroke-width="0.8" opacity="0.6"/>
        <!-- Y Letter -->
        <text x="26" y="33" text-anchor="middle" font-family="Orbitron,sans-serif" font-size="18" font-weight="900" fill="url(#lg1)" filter="url(#glow)">Y</text>
        <!-- Corner dots -->
        <circle cx="6" cy="12" r="2" fill="#00E5FF" opacity="0.8"/>
        <circle cx="46" cy="12" r="2" fill="#8A2BE2" opacity="0.8"/>
        <circle cx="26" cy="49" r="2" fill="#00FF6A" opacity="0.8"/>
        <!-- Top bar accent -->
        <line x1="14" y1="2" x2="38" y2="2" stroke="url(#lg2)" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
      <div class="logo-text">
        <span class="logo-main">YAMIN AHMED</span>
        <span class="logo-sub">eFootball Vlog</span>
      </div>
    </a>
    <div class="nav-actions">
      <button class="btn-admin" onclick="openAdmin()">⚙ ADMIN</button>
      <a href="https://www.youtube.com/@YaminOfficial-iq3wp" target="_blank" class="btn-yt">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M21.8 8s-.2-1.4-.8-2c-.8-.8-1.6-.8-2-.9C16.3 5 12 5 12 5s-4.3 0-7 .1c-.4.1-1.2.1-2 .9-.6.6-.8 2-.8 2S2 9.6 2 11.2v1.5c0 1.6.2 3.2.2 3.2s.2 1.4.8 2c.8.8 1.8.8 2.3.9C7 19 12 19 12 19s4.3 0 7-.2c.4-.1 1.2-.1 2-.9.6-.6.8-2 .8-2s.2-1.6.2-3.2v-1.5C22 9.6 21.8 8 21.8 8zM10 14.5v-5l5.5 2.5L10 14.5z"/></svg>
        Follow on YouTube
      </a>
    </div>
  </div>
  <nav class="nav-scroll">
    <div class="nav-links">
      <a href="#home">Home</a>
      <a href="#updates">News & Patch Notes</a>
      <a href="#builds">Player Build Guides</a>
      <a href="#meta">Meta Playbook</a>
      <a href="#f2p">F2P Hidden Gems</a>
      <a href="#reviews">Manager Reviews</a>
      <a href="#skills">Skill Move Tutorial</a>
      <a href="#defending">Defending Masterclass</a>
      <a href="#squads">Squad Showcases</a>
      <a href="#form">Weekly Form Updates</a>
      <a href="#coins">Coin & GP Farming</a>
      <a href="#ai">AI Creative Studio</a>
      <a href="#compare">Comparison Tool</a>
      <a href="#formation">Formation Laboratory</a>
      <a href="#mobile">Mobile vs. Console</a>
      <a href="#tournament">Tournament Hub</a>
      <a href="#contact">Contact / Follow Me</a>
    </div>
  </nav>
</header>

<!-- HERO -->
<div class="hero" id="home">
  <div class="hero-glow"></div>
  <div class="hero-glow2"></div>
  <div class="hero-content">
    <div class="hero-badge">⚡ eFootball Community Hub</div>
    <h1>Welcome to<br><span>Yamin Ahmed</span> Vlog</h1>
    <p class="hero-sub">Your Home for eFootball Tips, News & Meta Guides</p>
    <a href="https://www.youtube.com/@YaminOfficial-iq3wp" target="_blank" class="btn-subscribe">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M21.8 8s-.2-1.4-.8-2c-.8-.8-1.6-.8-2-.9C16.3 5 12 5 12 5s-4.3 0-7 .1c-.4.1-1.2.1-2 .9-.6.6-.8 2-.8 2S2 9.6 2 11.2v1.5c0 1.6.2 3.2.2 3.2s.2 1.4.8 2c.8.8 1.8.8 2.3.9C7 19 12 19 12 19s4.3 0 7-.2c.4-.1 1.2-.1 2-.9.6-.6.8-2 .8-2s.2-1.6.2-3.2v-1.5C22 9.6 21.8 8 21.8 8zM10 14.5v-5l5.5 2.5L10 14.5z"/></svg>
      Subscribe on YouTube
    </a>
    <div class="hero-stats">
      <div class="stat">
        <div class="stat-num" id="postCount">0</div>
        <div class="stat-label">Posts</div>
      </div>
      <div class="stat">
        <div class="stat-num">17</div>
        <div class="stat-label">Categories</div>
      </div>
      <div class="stat">
        <div class="stat-num" id="showcaseCount">0</div>
        <div class="stat-label">Showcases</div>
      </div>
    </div>
  </div>
</div>

<div class="neon-sep"></div>

<!-- LATEST UPDATES -->
<section id="updates">
  <div class="section-header">
    <div class="section-label">📡 Feed</div>
    <div class="section-title">Latest Updates</div>
  </div>
  <div class="cards-grid" id="postsGrid">
    <div class="empty-state" style="grid-column:1/-1">
      <div class="big-icon">📭</div>
      No posts yet — open the Admin Panel to add your first post!
    </div>
  </div>
</section>

<div class="neon-sep"></div>

<!-- TOP CATEGORIES -->
<section>
  <div class="section-header">
    <div class="section-label">🗂️ Browse</div>
    <div class="section-title">Top Categories</div>
  </div>
  <div class="cats-grid">
    <a href="#updates" class="cat-btn"><span class="cat-icon">📰</span><span class="cat-name">News & Patch Notes</span></a>
    <a href="#builds" class="cat-btn"><span class="cat-icon">⚡</span><span class="cat-name">Player Build Guides</span></a>
    <a href="#meta" class="cat-btn"><span class="cat-icon">📖</span><span class="cat-name">Meta Playbook</span></a>
    <a href="#f2p" class="cat-btn"><span class="cat-icon">💎</span><span class="cat-name">F2P Hidden Gems</span></a>
    <a href="#reviews" class="cat-btn"><span class="cat-icon">🧠</span><span class="cat-name">Manager Reviews</span></a>
    <a href="#skills" class="cat-btn"><span class="cat-icon">🎯</span><span class="cat-name">Skill Move Tutorial</span></a>
    <a href="#defending" class="cat-btn"><span class="cat-icon">🛡️</span><span class="cat-name">Defending Masterclass</span></a>
    <a href="#squads" class="cat-btn"><span class="cat-icon">👥</span><span class="cat-name">Squad Showcases</span></a>
    <a href="#form" class="cat-btn"><span class="cat-icon">📈</span><span class="cat-name">Weekly Form Updates</span></a>
    <a href="#coins" class="cat-btn"><span class="cat-icon">🪙</span><span class="cat-name">Coin & GP Farming</span></a>
    <a href="#ai" class="cat-btn"><span class="cat-icon">🤖</span><span class="cat-name">AI Creative Studio</span></a>
    <a href="#compare" class="cat-btn"><span class="cat-icon">⚖️</span><span class="cat-name">Comparison Tool</span></a>
    <a href="#formation" class="cat-btn"><span class="cat-icon">🔬</span><span class="cat-name">Formation Laboratory</span></a>
    <a href="#mobile" class="cat-btn"><span class="cat-icon">📱</span><span class="cat-name">Mobile vs. Console</span></a>
    <a href="#tournament" class="cat-btn"><span class="cat-icon">🏆</span><span class="cat-name">Tournament Hub</span></a>
    <a href="#contact" class="cat-btn"><span class="cat-icon">📬</span><span class="cat-name">Contact / Follow Me</span></a>
  </div>
</section>

<div class="neon-sep"></div>

<!-- FEATURED VIDEO -->
<section id="featured">
  <div class="section-header">
    <div class="section-label">🎬 Watch</div>
    <div class="section-title">Featured Video</div>
  </div>
  <div class="featured-video-wrap" id="featuredVideoWrap">
    <div class="video-embed">
      <iframe id="featuredIframe" src="https://www.youtube.com/embed/videoseries?list=UU&controls=1" allowfullscreen></iframe>
    </div>
    <div class="video-info">
      <span class="video-tag">⭐ FEATURED</span>
      <div class="video-title" id="featuredTitle">Subscribe for Latest eFootball Content</div>
      <p class="video-desc" id="featuredDesc">Check out Yamin Ahmed's latest eFootball guides, meta tips, and squad builds. New content drops regularly — hit subscribe so you never miss an update!</p>
    </div>
  </div>
</section>

<div class="neon-sep"></div>

<!-- COMMUNITY / SQUAD SHOWCASES -->
<section id="squads">
  <div class="section-header">
    <div class="section-label">🏟️ Community</div>
    <div class="section-title">Squad Showcases</div>
  </div>
  <div class="community-actions">
    <p style="color:rgba(234,234,234,0.6);font-size:0.85rem;font-family:'Rajdhani',sans-serif;">Share your best squad builds with the community!</p>
    <button class="btn-upload" onclick="openAdmin('squad')">+ Upload Your Squad</button>
  </div>
  <div class="showcase-grid" id="showcaseGrid">
    <div class="empty-state" style="grid-column:1/-1">
      <div class="big-icon">⚽</div>
      No squads submitted yet — be the first!
    </div>
  </div>
</section>

<div class="neon-sep"></div>

<!-- FOOTER -->
<footer id="contact">
  <div class="footer-grid">
    <div class="footer-about">
      <div style="display:flex;align-items:center;gap:0.8rem;margin-bottom:0.8rem;">
        <svg width="36" height="36" viewBox="0 0 52 52" fill="none">
          <path d="M26 3 L46 12 L46 28 C46 38 36 46 26 49 C16 46 6 38 6 28 L6 12 Z" fill="#8A2BE2" opacity="0.3" stroke="#00E5FF" stroke-width="1.5"/>
          <text x="26" y="33" text-anchor="middle" font-family="Orbitron" font-size="18" font-weight="900" fill="#00E5FF">Y</text>
        </svg>
        <div>
          <div style="font-family:'Orbitron',sans-serif;font-size:0.9rem;color:var(--neon-blue);font-weight:700;">YAMIN AHMED VLOG</div>
          <div style="font-family:'Rajdhani',sans-serif;font-size:0.65rem;color:var(--green);letter-spacing:2px;">eFootball Community</div>
        </div>
      </div>
      <p>Your #1 destination for eFootball tips, meta guides, squad builds, and the latest patch notes. Stay ahead of the competition.</p>
    </div>
    <div>
      <div class="footer-title">QUICK LINKS</div>
      <ul class="footer-links">
        <li><a href="#">News & Patch Notes</a></li>
        <li><a href="#">Player Build Guides</a></li>
        <li><a href="#">Meta Playbook</a></li>
        <li><a href="#">Coin & GP Farming</a></li>
        <li><a href="#">Tournament Hub</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-title">FOLLOW ME</div>
      <ul class="footer-links">
        <li><a href="https://www.youtube.com/@YaminOfficial-iq3wp" target="_blank">▶ YouTube Channel</a></li>
        <li><a href="#">📧 Contact Me</a></li>
      </ul>
      <div style="margin-top:1.5rem;">
        <a href="https://www.youtube.com/@YaminOfficial-iq3wp" target="_blank" class="btn-yt" style="display:inline-flex;">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M21.8 8s-.2-1.4-.8-2c-.8-.8-1.6-.8-2-.9C16.3 5 12 5 12 5s-4.3 0-7 .1c-.4.1-1.2.1-2 .9-.6.6-.8 2-.8 2S2 9.6 2 11.2v1.5c0 1.6.2 3.2.2 3.2s.2 1.4.8 2c.8.8 1.8.8 2.3.9C7 19 12 19 12 19s4.3 0 7-.2c.4-.1 1.2-.1 2-.9.6-.6.8-2 .8-2s.2-1.6.2-3.2v-1.5C22 9.6 21.8 8 21.8 8zM10 14.5v-5l5.5 2.5L10 14.5z"/></svg>
          Subscribe
        </a>
      </div>
    </div>
  </div>
  <div class="footer-bottom">
    © 2025 Yamin Ahmed Vlog. All Rights Reserved. Built with ⚡ for the eFootball Community.
  </div>
</footer>

<!-- ============ PASSWORD MODAL ============ -->
<div class="pw-overlay" id="pwOverlay">
  <div class="pw-box">
    <div class="pw-icon">🔐</div>
    <div class="pw-title">ADMIN ACCESS</div>
    <div class="pw-sub">Enter your admin password to continue</div>
    <div class="pw-input-wrap">
      <input class="pw-input" type="password" id="pwInput" placeholder="••••••••" maxlength="20"
        onkeydown="if(event.key==='Enter')checkPassword()">
      <button class="pw-toggle" onclick="togglePwVis()" id="pwToggleBtn" title="Show/hide">👁</button>
    </div>
    <div class="pw-err" id="pwErr"></div>
    <button class="pw-submit" onclick="checkPassword()">UNLOCK ADMIN ⚡</button>
    <br>
    <button class="pw-cancel" onclick="closePwModal()">✕ Cancel</button>
    <div class="pw-attempts" id="pwAttempts"></div>
  </div>
</div>

<!-- ============ ADMIN OVERLAY ============ -->
<div class="admin-overlay" id="adminOverlay">
  <div class="admin-panel">
    <div class="admin-header">
      <h2>⚙ ADMIN PANEL</h2>
      <button class="admin-close" onclick="closeAdmin()">✕</button>
    </div>
    <div class="admin-body">

      <!-- TABS -->
      <div class="admin-tabs">
        <button class="admin-tab active" onclick="switchTab('post')">📝 Add Post</button>
        <button class="admin-tab" onclick="switchTab('squad')">⚽ Add Squad Showcase</button>
        <button class="admin-tab" onclick="switchTab('video')">🎬 Featured Video</button>
        <button class="admin-tab" onclick="switchTab('manage')">🗂 Manage Posts</button>
      </div>

      <!-- TAB: ADD POST -->
      <div class="tab-content active" id="tab-post">
        <div class="form-group">
          <label>Category</label>
          <select id="postCategory">
            <option value="News & Patch Notes">📰 News & Patch Notes</option>
            <option value="Player Build Guides">⚡ Player Build Guides</option>
            <option value="Meta Playbook">📖 Meta Playbook</option>
            <option value="F2P Hidden Gems">💎 F2P Hidden Gems</option>
            <option value="Manager Reviews">🧠 Manager Reviews</option>
            <option value="Skill Move Tutorial">🎯 Skill Move Tutorial</option>
            <option value="Defending Masterclass">🛡️ Defending Masterclass</option>
            <option value="Squad Showcases">👥 Squad Showcases</option>
            <option value="Weekly Form Updates">📈 Weekly Form Updates</option>
            <option value="Coin & GP Farming">🪙 Coin & GP Farming</option>
            <option value="AI Creative Studio">🤖 AI Creative Studio</option>
            <option value="Comparison Tool">⚖️ Comparison Tool</option>
            <option value="Formation Laboratory">🔬 Formation Laboratory</option>
            <option value="Mobile vs. Console">📱 Mobile vs. Console</option>
            <option value="Tournament Hub">🏆 Tournament Hub</option>
          </select>
        </div>
        <div class="form-group">
          <label>Post Title</label>
          <input type="text" id="postTitle" placeholder="e.g. Best Meta Formation for Patch 4.5.0">
        </div>
        <div class="form-group">
          <label>Description / Summary</label>
          <textarea id="postDesc" placeholder="Write a short summary of the post content..."></textarea>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label>Link (optional)</label>
            <input type="url" id="postLink" placeholder="https://...">
          </div>
          <div class="form-group">
            <label>Date</label>
            <input type="date" id="postDate">
          </div>
        </div>
        <div class="form-group">
          <label>Post Image (JPEG/PNG — shown next to your post)</label>
          <div class="image-upload-area" onclick="document.getElementById('postImageInput').click()">
            <input type="file" id="postImageInput" accept="image/jpeg,image/png,image/webp" onchange="previewImage(this,'postImagePreview','postImageData')">
            <div class="upload-icon">🖼️</div>
            <div class="upload-text">Drop image here or <span>click to browse</span><br><small style="opacity:0.5">JPEG · PNG · WebP</small></div>
          </div>
          <div class="image-preview-box" id="postImagePreview">
            <img id="postImageData" src="" alt="preview">
          </div>
        </div>
        <button class="btn-submit" onclick="addPost()">✚ PUBLISH POST</button>
      </div>

      <!-- TAB: SQUAD SHOWCASE -->
      <div class="tab-content" id="tab-squad">
        <div class="form-group">
          <label>Squad Name / Title</label>
          <input type="text" id="squadTitle" placeholder="e.g. Insane F2P La Liga Squad – 4-3-3">
        </div>
        <div class="form-group">
          <label>Description</label>
          <textarea id="squadDesc" placeholder="Describe the squad, formation, key players..."></textarea>
        </div>
        <div class="form-group">
          <label>Submitted By</label>
          <input type="text" id="squadAuthor" placeholder="Your name / username">
        </div>
        <div class="form-group">
          <label>Squad Image (JPEG/PNG — screenshot of your squad)</label>
          <div class="image-upload-area" onclick="document.getElementById('squadImageInput').click()">
            <input type="file" id="squadImageInput" accept="image/jpeg,image/png,image/webp" onchange="previewImage(this,'squadImagePreview','squadImageData')">
            <div class="upload-icon">📸</div>
            <div class="upload-text">Upload your squad screenshot<br><span>click to browse</span><br><small style="opacity:0.5">JPEG · PNG · WebP</small></div>
          </div>
          <div class="image-preview-box" id="squadImagePreview">
            <img id="squadImageData" src="" alt="preview">
          </div>
        </div>
        <button class="btn-submit" onclick="addSquad()">✚ SUBMIT SQUAD</button>
      </div>

      <!-- TAB: FEATURED VIDEO -->
      <div class="tab-content" id="tab-video">
        <div class="form-group">
          <label>YouTube Video ID or URL</label>
          <input type="text" id="videoUrl" placeholder="e.g. dQw4w9WgXcQ or full YouTube URL">
        </div>
        <div class="form-group">
          <label>Video Title</label>
          <input type="text" id="videoTitle" placeholder="e.g. Ultimate eFootball Meta Guide 2025">
        </div>
        <div class="form-group">
          <label>Video Description</label>
          <textarea id="videoDesc" placeholder="Describe what this video covers..."></textarea>
        </div>
        <button class="btn-submit" onclick="setFeaturedVideo()">✚ SET FEATURED VIDEO</button>
      </div>

      <!-- TAB: MANAGE -->
      <div class="tab-content" id="tab-manage">
        <div style="font-family:'Rajdhani',sans-serif;font-size:0.8rem;color:rgba(234,234,234,0.5);margin-bottom:1rem;letter-spacing:1px;">ALL PUBLISHED POSTS</div>
        <div id="adminPostsList">
          <div class="empty-state"><div class="big-icon">📭</div>No posts yet</div>
        </div>
        <div style="font-family:'Rajdhani',sans-serif;font-size:0.8rem;color:rgba(234,234,234,0.5);margin:2rem 0 1rem;letter-spacing:1px;">SQUAD SHOWCASES</div>
        <div id="adminSquadsList">
          <div class="empty-state"><div class="big-icon">⚽</div>No squads yet</div>
        </div>
      </div>

    </div>
  </div>
</div>

<!-- TOAST -->
<div class="success-toast" id="toast">✅ Saved!</div>

<script>
// ============ DATA STORE ============
let posts = JSON.parse(localStorage.getItem('yamin_posts') || '[]');
let squads = JSON.parse(localStorage.getItem('yamin_squads') || '[]');

function save() {
  localStorage.setItem('yamin_posts', JSON.stringify(posts));
  localStorage.setItem('yamin_squads', JSON.stringify(squads));
}

// Set default date
document.getElementById('postDate').valueAsDate = new Date();

// ============ RENDER ============
function renderPosts() {
  const grid = document.getElementById('postsGrid');
  document.getElementById('postCount').textContent = posts.length;
  if (!posts.length) {
    grid.innerHTML = '<div class="empty-state" style="grid-column:1/-1"><div class="big-icon">📭</div>No posts yet — open the Admin Panel to add your first post!</div>';
    return;
  }
  grid.innerHTML = posts.slice().reverse().map((p, i) => {
    const imgHtml = p.image
      ? `<div class="card-img" style="position:relative;overflow:hidden;height:180px;"><img src="${p.image}" alt="${p.title}" style="width:100%;height:180px;object-fit:cover;"><span class="card-tag">${p.category}</span></div>`
      : `<div class="card-img-placeholder" style="position:relative;"><span style="font-size:2.5rem;">${getCatIcon(p.category)}</span><span class="card-tag" style="position:absolute;top:10px;left:10px;">${p.category}</span></div>`;
    const link = p.link ? `href="${p.link}" target="_blank"` : 'href="#"';
    return `
      <a ${link} class="post-card" style="text-decoration:none;">
        ${imgHtml}
        <div class="card-body">
          <div class="card-title">${p.title}</div>
          <div class="card-desc">${p.desc || ''}</div>
          <div class="card-meta"><span>${p.date || ''}</span><span style="color:var(--neon-blue)">→ Read More</span></div>
        </div>
      </a>`;
  }).join('');
}

function renderSquads() {
  const grid = document.getElementById('showcaseGrid');
  document.getElementById('showcaseCount').textContent = squads.length;
  if (!squads.length) {
    grid.innerHTML = '<div class="empty-state" style="grid-column:1/-1"><div class="big-icon">⚽</div>No squads submitted yet — be the first!</div>';
    return;
  }
  grid.innerHTML = squads.slice().reverse().map((s, ri) => {
    const idx = squads.length - 1 - ri;
    const imgHtml = s.image
      ? `<div class="showcase-img"><img src="${s.image}" alt="${s.title}"></div>`
      : `<div class="showcase-img">⚽</div>`;
    return `
      <div class="showcase-card">
        ${imgHtml}
        <div class="showcase-body">
          <div class="showcase-title">${s.title}</div>
          <div style="font-size:0.75rem;color:rgba(234,234,234,0.5);font-family:'Rajdhani',sans-serif;margin-bottom:0.5rem;">by ${s.author || 'Anonymous'}</div>
          <div style="font-size:0.78rem;color:rgba(234,234,234,0.6);margin-bottom:0.8rem;">${s.desc || ''}</div>
          <div class="upvote-row">
            <button class="upvote-btn ${s.upvoted?'active':''}" onclick="upvote(${idx}, this)">▲ Upvote</button>
            <span class="upvote-count">${s.votes || 0}</span>
          </div>
        </div>
      </div>`;
  }).join('');
}

function renderAdminLists() {
  // Posts
  const pl = document.getElementById('adminPostsList');
  if (!posts.length) {
    pl.innerHTML = '<div class="empty-state"><div class="big-icon">📭</div>No posts yet</div>';
  } else {
    pl.innerHTML = posts.map((p, i) => `
      <div class="admin-post-item">
        <div class="admin-post-thumb">${p.image ? `<img src="${p.image}">` : getCatIcon(p.category)}</div>
        <div class="admin-post-info">
          <div class="admin-post-title">${p.title}</div>
          <div class="admin-post-cat">${p.category} · ${p.date || ''}</div>
        </div>
        <div class="admin-post-actions">
          <button class="btn-delete" onclick="deletePost(${i})">✕ Delete</button>
        </div>
      </div>`).join('');
  }
  // Squads
  const sl = document.getElementById('adminSquadsList');
  if (!squads.length) {
    sl.innerHTML = '<div class="empty-state"><div class="big-icon">⚽</div>No squads yet</div>';
  } else {
    sl.innerHTML = squads.map((s, i) => `
      <div class="admin-post-item">
        <div class="admin-post-thumb">${s.image ? `<img src="${s.image}">` : '⚽'}</div>
        <div class="admin-post-info">
          <div class="admin-post-title">${s.title}</div>
          <div class="admin-post-cat">by ${s.author || 'Anon'} · ${s.votes || 0} votes</div>
        </div>
        <div class="admin-post-actions">
          <button class="btn-delete" onclick="deleteSquad(${i})">✕ Delete</button>
        </div>
      </div>`).join('');
  }
}

function getCatIcon(cat) {
  const map = {
    'News & Patch Notes':'📰','Player Build Guides':'⚡','Meta Playbook':'📖',
    'F2P Hidden Gems':'💎','Manager Reviews':'🧠','Skill Move Tutorial':'🎯',
    'Defending Masterclass':'🛡️','Squad Showcases':'👥','Weekly Form Updates':'📈',
    'Coin & GP Farming':'🪙','AI Creative Studio':'🤖','Comparison Tool':'⚖️',
    'Formation Laboratory':'🔬','Mobile vs. Console':'📱','Tournament Hub':'🏆'
  };
  return map[cat] || '📄';
}

// ============ ADMIN ACTIONS ============
function addPost() {
  const title = document.getElementById('postTitle').value.trim();
  if (!title) { alert('Please enter a post title.'); return; }
  posts.push({
    title,
    category: document.getElementById('postCategory').value,
    desc: document.getElementById('postDesc').value.trim(),
    link: document.getElementById('postLink').value.trim(),
    date: document.getElementById('postDate').value,
    image: document.getElementById('postImageData').src || null
  });
  save(); renderPosts(); renderAdminLists();
  // reset
  document.getElementById('postTitle').value = '';
  document.getElementById('postDesc').value = '';
  document.getElementById('postLink').value = '';
  document.getElementById('postImageData').src = '';
  document.getElementById('postImagePreview').classList.remove('show');
  showToast('✅ Post published!');
}

function addSquad() {
  const title = document.getElementById('squadTitle').value.trim();
  if (!title) { alert('Please enter a squad title.'); return; }
  squads.push({
    title,
    desc: document.getElementById('squadDesc').value.trim(),
    author: document.getElementById('squadAuthor').value.trim(),
    image: document.getElementById('squadImageData').src || null,
    votes: 0,
    upvoted: false
  });
  save(); renderSquads(); renderAdminLists();
  document.getElementById('squadTitle').value = '';
  document.getElementById('squadDesc').value = '';
  document.getElementById('squadAuthor').value = '';
  document.getElementById('squadImageData').src = '';
  document.getElementById('squadImagePreview').classList.remove('show');
  showToast('✅ Squad submitted!');
}

function setFeaturedVideo() {
  let url = document.getElementById('videoUrl').value.trim();
  const title = document.getElementById('videoTitle').value.trim();
  const desc = document.getElementById('videoDesc').value.trim();

  // extract video ID
  let vid = url;
  const match = url.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/|embed\/)([a-zA-Z0-9_-]{11})/);
  if (match) vid = match[1];

  document.getElementById('featuredIframe').src = `https://www.youtube.com/embed/${vid}`;
  if (title) document.getElementById('featuredTitle').textContent = title;
  if (desc) document.getElementById('featuredDesc').textContent = desc;
  showToast('✅ Featured video updated!');
}

function deletePost(i) {
  if (!confirm('Delete this post?')) return;
  posts.splice(i, 1);
  save(); renderPosts(); renderAdminLists();
  showToast('🗑️ Post deleted.');
}

function deleteSquad(i) {
  if (!confirm('Delete this squad?')) return;
  squads.splice(i, 1);
  save(); renderSquads(); renderAdminLists();
  showToast('🗑️ Squad deleted.');
}

function upvote(idx, btn) {
  squads[idx].upvoted = !squads[idx].upvoted;
  squads[idx].votes = (squads[idx].votes || 0) + (squads[idx].upvoted ? 1 : -1);
  save(); renderSquads();
}

// ============ IMAGE PREVIEW ============
function previewImage(input, previewBoxId, imgId) {
  const file = input.files[0];
  if (!file) return;
  const reader = new FileReader();
  reader.onload = e => {
    document.getElementById(imgId).src = e.target.result;
    document.getElementById(previewBoxId).classList.add('show');
  };
  reader.readAsDataURL(file);
}

// ============ PASSWORD ============
const ADMIN_PASSWORD = 'EFB2026';
let pwAttempts = 0;
let pwLocked = false;
let pendingTab = null;

function openAdmin(tab) {
  pendingTab = tab || null;
  if (pwLocked) {
    showToast('⛔ Too many attempts. Try again later.');
    return;
  }
  // Show password modal
  document.getElementById('pwOverlay').classList.add('open');
  document.getElementById('pwInput').value = '';
  document.getElementById('pwErr').textContent = '';
  document.getElementById('pwAttempts').textContent = '';
  document.body.style.overflow = 'hidden';
  setTimeout(() => document.getElementById('pwInput').focus(), 100);
}

function checkPassword() {
  const val = document.getElementById('pwInput').value;
  const errEl = document.getElementById('pwErr');
  const attEl = document.getElementById('pwAttempts');

  if (val === ADMIN_PASSWORD) {
    // CORRECT
    pwAttempts = 0;
    closePwModal();
    openAdminPanel(pendingTab);
  } else {
    // WRONG
    pwAttempts++;
    const remaining = 5 - pwAttempts;
    document.getElementById('pwInput').classList.add('error');
    setTimeout(() => document.getElementById('pwInput').classList.remove('error'), 500);
    document.getElementById('pwInput').value = '';

    if (pwAttempts >= 5) {
      pwLocked = true;
      errEl.textContent = '⛔ Too many failed attempts!';
      attEl.textContent = 'Admin locked for 5 minutes.';
      setTimeout(() => { pwLocked = false; pwAttempts = 0; }, 300000);
      closePwModal();
      document.body.style.overflow = '';
    } else {
      errEl.textContent = '❌ Incorrect password. Try again.';
      attEl.textContent = remaining > 0 ? `${remaining} attempt${remaining!==1?'s':''} remaining` : '';
    }
  }
}

function togglePwVis() {
  const inp = document.getElementById('pwInput');
  const btn = document.getElementById('pwToggleBtn');
  if (inp.type === 'password') {
    inp.type = 'text';
    btn.textContent = '🙈';
  } else {
    inp.type = 'password';
    btn.textContent = '👁';
  }
}

function closePwModal() {
  document.getElementById('pwOverlay').classList.remove('open');
  if (!document.getElementById('adminOverlay').classList.contains('open')) {
    document.body.style.overflow = '';
  }
}

function openAdminPanel(tab) {
  document.getElementById('adminOverlay').classList.add('open');
  document.body.style.overflow = 'hidden';
  if (tab) switchTab(tab);
  renderAdminLists();
}

// Close pw modal on overlay click
document.getElementById('pwOverlay').addEventListener('click', e => {
  if (e.target === document.getElementById('pwOverlay')) closePwModal();
});

function closeAdmin() {
  document.getElementById('adminOverlay').classList.remove('open');
  document.body.style.overflow = '';
}

function switchTab(name) {
  document.querySelectorAll('.admin-tab').forEach((t,i) => {
    const tabs = ['post','squad','video','manage'];
    t.classList.toggle('active', tabs[i] === name);
  });
  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
  document.getElementById(`tab-${name}`).classList.add('active');
  if (name === 'manage') renderAdminLists();
}

function showToast(msg) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 3000);
}

// Close on overlay click
document.getElementById('adminOverlay').addEventListener('click', e => {
  if (e.target === document.getElementById('adminOverlay')) closeAdmin();
});

// INIT
renderPosts();
renderSquads();
</script>
</body>
</html>
